HW2-1
先利用讀檔案的方式將檔案內的數字一個一個放入一個二維陣列temp中，再由band matrix的定義，將其拆成下半部(包含中間)以及上半部，並分別呼叫value，將其值放入一維陣列e[]中並印出。

HW2-2
先由玩家輸入一個合理的 n m 還有蟑螂的初始位置，如果不合理將會立刻結束，然後建立一個二維的動態記憶體arr[n][m]來計算每一點經過的次數，用rand隨機去呼叫0~7，再去call其位置，
如果蟑螂接下來的位置合理，就將其經過的位置加一，如果不合理就回去再call一個rand，call到合理為止，然後直到所有位置都經過，或是其步數超過50000，然後將其步數結果寫入Sample_output.txt
，最後將動態記憶體還回去。